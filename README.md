# Key-frame-css
CSS Keyframes CSS keyframes define the intermediate steps in an animation sequence. They allow you to control how an element changes style over time — from start to finish — by specifying percentage-based checkpoints. Each keyframe describes the styles that should apply at that moment in the animation timeline.
